You must fully complete i18n integration in this project and ensure it is clean, consistent, and self-verified. 
Follow this exact order strictly:

### STEP 1 — CLEANUP
- Delete unused/backup/duplicate/test files (keep only what’s actively used).
- Confirm project builds after cleanup.

### STEP 2 — LOCALE FILES
- Use English as the master reference (do not touch English UI).
- Compare every other locale with English → add missing keys.
- Always provide real translations (never English fallback; if impossible, use key itself).
- Do not rename or delete existing keys.

### STEP 3 — REMOVE HARDCODED ENGLISH
- Scan all 80+ pages and components.
- Replace all hardcoded English text (buttons, labels, acronyms, headings, etc.) with i18n keys.
- If key exists → use it. If not → create a new one and translate it in all languages.

### STEP 4 — NAMING CONVENTION
- Invent a standardized convention **based on existing keys**.
- Apply only to new keys. Never change existing keys.

### STEP 5 — PRIORITY PAGES
- First handle Services category pages, Product category pages, and Product subcategory pages.
- Then handle all remaining pages.

### STEP 6 — VERIFY & BUILD
- Ensure no hardcoded English remains.
- All 10 locales complete, validated, and synchronized.
- Acronyms (PLC, SCADA, BLDC, etc.) localized.
- No duplicate keys.
- Build (`npm run build`) must pass without i18n errors.
- Language switching and RTL (Arabic) margins/paddings must work.

### STEP 7 — RE-VALIDATE
- Rescan entire project for leftover English.
- Re-compare locales with English master.
- Confirm full synchronization before declaring completion.

### FINAL DELIVERABLES
1. Summary report (keys added/updated, files changed, files deleted with reasons).
2. Proof all 10 locales complete & validated.
3. Build confirmation.
4. Re-validation confirmation.
5. Remaining TODOs (if any).

### RULES
- Never use English fallback.
- Never rename existing keys.
- Follow steps until full completion.
- Ask me questions if something is unclear before proceeding.
