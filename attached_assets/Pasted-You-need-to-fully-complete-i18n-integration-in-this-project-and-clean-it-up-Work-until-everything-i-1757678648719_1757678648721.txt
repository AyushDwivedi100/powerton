You need to fully complete i18n integration in this project and clean it up. Work until everything is 100% complete and self-verified.

### 🔹 Tasks

1. **Remove Hardcoded English Text**
   - Search the entire project (all 80+ pages, all components, every file).
   - Replace *every hardcoded English string* (buttons, labels, addresses, headings, short forms, acronyms like PLC, SCADA, BLDC, etc.) with a **unique i18n key** following the project’s conventions.
   - No English text should remain hardcoded anywhere.

2. **Complete All Locale Files (10 Languages)**
   - Ensure every i18n key exists in **all 10 locale files**.
   - If a translation is missing in any language, provide a **real translated value** for that language.
   - If it is truly impossible to translate, then use the **translation key itself** as a placeholder (never English).
   - Locale files must be **consistent, validated, and error-free**.
   - Remove duplicate keys and maintain clean, organized structure.

3. **Acronym Handling**
   - Translate acronyms (PLC, SCADA, BLDC, etc.) into all 10 languages if they have proper equivalents.
   - If no equivalent exists in a language, provide the best localized representation (not English).

4. **Cleanup of Unused Files/Folders**
   - Identify and remove any unnecessary or unused files/folders (backups, duplicates, test junk, old assets).
   - Do not delete anything actively or passively required by the project.
   - Confirm the app builds successfully after cleanup.

5. **Fix Errors & Validate Build**
   - Fix any i18n errors, warnings, or issues caused by these changes.
   - Run build/test commands (like `npm run build`) and ensure everything works without i18n-related errors.
   - Confirm language switching and RTL layout (Arabic) still work correctly.

### 🔹 Rules

- Do **not** use English as a fallback language under any condition.
- Acronyms must also be translated (not left in English).
- Only ask questions if something is genuinely unclear.
- Do not stop early. You must continue working until:
  - ✅ No hardcoded English exists in **any file**.
  - ✅ Every key has translations in **all 10 languages**.
  - ✅ Locale files are fully validated and synchronized.
- **You are not allowed to say “all done” or stop until a re-validation scan is completed.**
- After declaring completion, perform a **mandatory re-scan of the entire project**:
  - Search for any leftover hardcoded English.
  - Verify all locale files are still complete and synchronized.
  - Only if the re-scan confirms everything is correct can you say the work is finished.

### 🔹 Final Deliverables

At the end, provide me with:
1. A **summary report** of:
   - Keys added, updated, or cleaned.
   - Number of files changed.
   - Files/folders deleted (with reasons).
2. Proof that all 10 locale files are complete, validated, and synchronized.
3. Confirmation that the project builds successfully after changes.
4. Confirmation that the **re-validation scan** passed with no issues.
5. Any remaining TODOs (only if something absolutely requires human input).
