echo "Replit Agent v3: Fully fix multilingual responsiveness and RTL/LTR support in this project. Work until everything is 100% complete, self-verified, and consistently executed.

1️⃣ Skip English Completely:
- English UI is perfect. Do NOT edit, style, or modify any 'en' locale files or English UI.
- Apply all fixes ONLY to non-English languages: Chinese, Hindi, Portuguese, Spanish, Arabic, French, Russian, Swahili, Hausa.

2️⃣ RTL / LTR Layout Support:
- Automatically set dir='rtl' for Arabic and dir='ltr' for all other languages.
- Flip all layout containers (navbars, sidebars, footers, modals, dropdowns, cards) fully for RTL.
- Icons, arrows, and directional components must be mirrored in Arabic.
- Margins, paddings, and text alignment must flip correctly.

3️⃣ Responsiveness & Long Translations:
- Ensure no text overflows, breaks, or pushes UI out of screen.
- Use Tailwind utilities: break-words, whitespace-normal, text-ellipsis, flex-wrap, truncate, max-w, min-w.
- Adjust spacing, wrapping, truncation, and container sizes WITHOUT changing fonts.
- Both desktop and mobile views must be fully responsive and consistent.

4️⃣ UI Standardization & Consistency:
- All visual elements must match English exactly: spacing, alignment, sizing, font-weight, component behavior.
- Apply fixes consistently across all 80+ pages/components.
- Arabic RTL must be fully mirrored visually.
- Non-English LTR languages must maintain proper spacing and alignment without breaking UI.

5️⃣ Validation & Testing:
- Re-scan the entire project after changes.
- Test all supported non-English languages in LTR and RTL.
- Confirm: No layout issues, no overflowing text, full RTL/LTR flip, icons/arrows flip correctly in Arabic, responsiveness preserved, UI consistency maintained, English untouched.

6️⃣ Screenshots for Verification:
- Take screenshots of all 80+ pages for every non-English language.
- Show desktop + mobile views.
- For Arabic, show fully mirrored RTL alignment.

7️⃣ Strict Rules:
- Do NOT remove or break translations or i18n setup.
- Do NOT modify English pages or 'en' locale files; build must fail if touched.
- Only modify styling/markup for non-English languages.
- Only ask questions if genuinely unclear; no unnecessary questions.
- Do not stop until: no text overflows, RTL behaves as expected, UI standardization fully maintained, build passes without errors.
- After completion, run mandatory full re-scan to verify no issues remain.

8️⃣ Deliverables:
- Report of updated files/components.
- Confirmation all non-English languages work in LTR and RTL.
- Proof of successful build after changes.
- Confirmation re-validation scan passed with no issues.
- Confirmation English UI and 'en' locale files untouched.
- Screenshots of all non-English languages (desktop + mobile, RTL proof for Arabic).
- Confirmation UI consistency and standardization fully maintained."
